kind: pipeline
type: docker
name: Default

steps:
  - name: rsync
    image: drillster/drone-rsync
    environment:
      RSYNC_KEY:
        from_secret: pkey
    settings:
      user: root
      hosts:
        - ss.think2011.net
      port: 22
      source: ./fff.txt
      target: ~/tmp/
