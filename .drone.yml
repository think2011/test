kind: pipeline
type: docker
name: Default

steps:
  - name: rsync
    image: drillster/drone-rsync
    commands:
      - ls
    settings:
      user: root
      hosts:
        - remote1
      source: ./fff
      target: ~/tmp/
